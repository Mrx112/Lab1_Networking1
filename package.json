{
  "name": "homecloud-link-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Demo interaktif jaringan Home Server ↔ Warehouse (simulasi, config, pengujian, server_guard)",
  "scripts": {
    "dev": "npx --yes serve -l 3000 .",
    "deploy": "npx --yes vercel --prod"
  }
}
